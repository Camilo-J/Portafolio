---
const { class: className, as: Tag = 'div', title, href, ...props } = Astro.props;
---

<Tag class="flex flex-row items-center justify-center gap-x-2 hover:opacity-75 transition-all" href={href} {...props}>
  <slot name="icon" />
  <p>{title}</p>
</Tag>
